# Core API — kept lean for Vercel serverless bundle (<15 MB)
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Dev / testing (not installed on Vercel; only used locally)
pytest>=8.0.0
